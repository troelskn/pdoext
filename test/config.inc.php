<?php

$GLOBALS['config']['db']['pgsql'] = null; // To enable postgres specific tests, you need to configure a postgres connection
                                          // the connection should be to an empty database where the may create and drop schema/tables
                                          // if null postgres specific tests are skipped
